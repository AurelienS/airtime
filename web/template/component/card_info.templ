package component

templ CardInfo(title string, color string) {
	<div class={ "bg-gray-900 rounded-lg p-5 w-full md:w-auto mt-4 shadow-2xl border border-gray-800", color }>
		<h2 class="text-xl font-bold mb-4">{ title }</h2>
		<div class="grid grid-cols-2 gap-2 font-light items-center">
			{ children... }
		</div>
	</div>
}
